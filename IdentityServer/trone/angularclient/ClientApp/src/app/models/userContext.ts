export interface IUserContext {
    Name: string,
    Email: string,
    FavColor: string,
    Gender: string,
    Birthdate: string,
    roles: [], //for some werid reason, making this prop name lower case in the only way to pour data in it
}
